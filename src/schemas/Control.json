{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "#Control",
  "description": "A security or IT Control. This is most likely an additional Class applied to a Service (e.g. Okta SSO), a Device (e.g. a physical firewall), or a HostAgent (e.g. Carbon Black CbDefense Agent).",
  "type": "object",
  "allOf": [
    {
      "$ref": "#Entity"
    },
    {
      "properties": {},
      "required": []
    }
  ]
}